import { MSubscriptionPackage } from './m-subscription-package.model';

describe('MSubscriptionPackage', () => {
  it('should create an instance', () => {
    expect(new MSubscriptionPackage()).toBeTruthy();
  });
});
