import { mRegisteredUsers } from './m-registered-users.model';

describe('mRegisteredUsers', () => {
  it('should create an instance', () => {
    expect(new mRegisteredUsers()).toBeTruthy();
  });
});
