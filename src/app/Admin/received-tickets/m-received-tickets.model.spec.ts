import { mReceivedTickets } from './m-received-tickets.model';

describe('mReceivedTickets', () => {
  it('should create an instance', () => {
    expect(new mReceivedTickets()).toBeTruthy();
  });
});
