import { mSuggestionReport } from './m-suggestion-report.model';

describe('mSuggestionReport', () => {
  it('should create an instance', () => {
    expect(new mSuggestionReport()).toBeTruthy();
  });
});
